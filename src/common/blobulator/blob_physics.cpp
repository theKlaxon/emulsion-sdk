#include "cbase.h"
#include "blob_physics.h"

void COpPtr_DoPhysics::Do(void* pData) {
	//PhysTiler* pTiler = PhysTilerFactory::factory->getTiler();
	//pTiler->beginFrame(Point3D(0.0f, 0.0f, 0.0f));
	//pTiler->endFrame();
	//PhysTilerFactory::factory->returnTiler(pTiler);

	//PhysParticle* ptest = new PhysParticle();
	
}